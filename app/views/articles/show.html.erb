<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article),
    method: :delete,
    # confirmation of delete, provided in rails apps.
    data: { confirm: "Are you sure?" } %>
  </li>
</ul>

<%# Displays comments ... removed for each using partial %>
<h2>Comments</h2>
<%= render @article.comments %>

<%# Creates a new form with array to build comments array %>
<%# Created the form in partial _form %>
<h2>Add a comment:</h2>
<%= render 'comments/form' %>


<%# Link to root_path %>
<%= link_to "Back to Articles", root_path %>